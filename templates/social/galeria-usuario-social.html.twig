{% extends 'layout.html.twig' %}

{% block title %}Reportaje{% endblock %}

{% block content %}
<div id="contenedor-global" class="pt-5 pb-0">
    <div class="container-fluid pb-0">
        <div class="seccion pt-5 text-light mr-4 ml-4 mt-2">
            <div class="col-md-10 alert alert-primary mx-auto text-center" role="alert">
                <p class="h3">Reportaje</p>
                <p>Haz click en las carpetas para acceder y ver su contenido.</p>
            </div>

            {# Contenedor Carpetas #}
            <div class="col-md-10 alert mx-auto text-center">
                <div class="row justify-content-center">
                    {% for carpeta in carpetas %}
                    <div class="col-md-2 col-sm-4 col-12 mb-3">
                        <a style="text-decoration: none; color: #ffffff;" href="{{ path('galeria-social-carpeta', {slug: carpeta}) }}">
                        <img class="img-fluid" src="{{ asset('build/images/red_folder.png') }}">
                        <p>{{carpeta}}</p>
                        </a>
                    </div>
                    {% endfor %}
                </div>
            </div>
            {# End Contenedor Carpetas #}
        </div>




        <div class="row" style="padding:0; margin:0; height:30vh;">
            <div class="malla"></div>
            <div class="bg-container" style="background: #a8a8a8;">
                <img width="100%" style="min-width: 600px" src="{{ asset('build/img/Inicio.jpg') }}">
            </div>
        </div>
    </div>
</div>

{% endblock %}
