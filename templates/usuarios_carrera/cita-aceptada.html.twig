{% extends 'layout.html.twig' %}
{% block logo %}
    {# include 'default/logo.html.twig' #}
{% endblock %}
{% block content %}
<div id="contenedor-global" class="pt-5 pb-0">
    <div class="container-fluid">
        <div class="seccion pt-5 text-light text-center">
            <div class="col-md-8 alert alert-primary mx-auto text-center" role="alert">
                <p class="mb-1">Tu cita ha sido <strong>registrada correctamente</strong> para el dia <strong>{{ cita.fechaCuadrante.fecha|date('d/m/Y') }}</strong> a las <strong>{{ cita.hora|date('H:i') }}</strong></p>
                <p class="mb-0">Si lo deseas puedes cancelarla en cualquier momento desde el menú citas</p>
            </div>
            <br/>
            <a class="mx-auto btn btn-success" href="{{ path('carrera-cita-usuario') }}">Continuar</a>
        </div>
        <br/>
            <div class="row" style="padding:0; margin:0; height:30vh;">
            <div class="malla"></div>
            <div class="bg-container" style="background: #a8a8a8;">
                <img width="100%" style="min-width: 600px" src="{{ asset('build/img/Inicio.jpg') }}">
            </div>
        </div>
    </div>
</div>

{% endblock %}
